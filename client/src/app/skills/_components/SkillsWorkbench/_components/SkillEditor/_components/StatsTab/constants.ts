/** Category bar tints — cycles when there are more categories than colors. */
export const CATEGORY_COLORS = [
  "var(--crit)",
  "var(--accent)",
  "var(--ok)",
  "var(--warn)",
  "var(--text-secondary)",
] as const;
