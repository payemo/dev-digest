import type { SkillType } from "@devdigest/shared";

/** Modal width, matching CreateAgentModal's. */
export const MODAL_WIDTH = 520;

/** Selectable types when creating a skill from scratch. */
export const SKILL_TYPE_VALUES: readonly SkillType[] = ["rubric", "convention", "security", "custom"];
