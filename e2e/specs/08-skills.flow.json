{
  "name": "Skills list renders a seeded skill and its Stats tab shows Used by",
  "description": "Loads /skills and confirms a seeded skill (API contract compatibility) is rendered. Opens it, switches to the Stats tab, and confirms the Used by metric label renders. Read-only against seeded data — no model call.",
  "steps": [
    { "cmd": ["open", "{BASE}/skills"], "label": "load the skills page" },
    { "cmd": ["wait", "--url", "/skills"], "label": "skills route reached" },
    { "cmd": ["wait", "--load", "networkidle"], "label": "skills fetch settles" },
    { "cmd": ["wait", "--text", "API contract compatibility"], "label": "seeded skill is visible in the list" },
    { "cmd": ["find", "text", "API contract compatibility", "click"], "label": "open the skill editor" },
    { "cmd": ["wait", "--url", "/skills/"], "label": "skill detail route reached" },
    { "cmd": ["wait", "--text", "Preview"], "label": "editor tabs render" },
    { "cmd": ["find", "role", "button", "click", "--name", "Stats"], "label": "switch to the Stats tab" },
    { "cmd": ["wait", "--text", "Used by"], "label": "stats tab shows the Used by metric" }
  ]
}
